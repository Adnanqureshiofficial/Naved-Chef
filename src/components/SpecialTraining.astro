<section class="training" id="specialized-training">
  <div class="container">

    <!-- =====================================================
         SECTION INTRO
         ===================================================== -->

    <header class="training__intro">
      <div>
        <span class="training__section-number">03</span>

        <p class="eyebrow">Professional Development</p>

        <h2 class="training__title font-display">
          Specialized
          <em>Training</em>
        </h2>
      </div>

      <p class="training__lead">
        Continued technical development through specialist chocolate
        education, hands-on workshops, and structured training with
        internationally recognised chocolate institutions.
      </p>
    </header>


    <!-- =====================================================
         CALLEBAUT
         ===================================================== -->

    <article class="training__institution training__institution--callebaut">

      <!-- Information -->
      <div class="training__details">

        <header class="training__brand">
          <div class="training__logo training__logo--callebaut">
            <!-- Replace with actual logo -->
            <span>LOGO</span>
          </div>

          <div>
            <p class="training__eyebrow">Chocolate Education</p>

            <h3 class="training__brand-name font-display">
              Callebaut
              <span>Chocolate Academy</span>
            </h3>
          </div>
        </header>


        <div class="training__overview">

          <p class="training__label">
            Training Overview
          </p>

          <p class="training__description">
            Specialist chocolate training focused on professional
            chocolate handling, confectionery techniques, moulded
            chocolate production, and contemporary pastry applications.
          </p>

        </div>


        <div class="training__skills">

          <p class="training__label">
            Core Skills
          </p>

          <ul class="training__skill-list">
            <li>Chocolate tempering</li>
            <li>Moulded chocolate</li>
            <li>Ganache &amp; confectionery</li>
            <li>Chocolate finishing</li>
            <li>Pastry applications</li>
            <li>Professional chocolate handling</li>
          </ul>

        </div>


        <div class="training__credential">
          <span class="training__credential-mark">✦</span>

          <div>
            <p class="training__label">
              Credential
            </p>

            <p class="training__credential-text">
              Professional Chocolate Training
            </p>
          </div>
        </div>

      </div>


      <!-- Workshop Gallery -->
      <div class="training__gallery-wrap">

        <div class="training__gallery-heading">
          <span>01</span>
          <p>Workshop / Practical</p>
        </div>

        <div class="training__gallery">

          <figure class="training__image">
            <div class="training__image-placeholder">
              <span>Workshop Photo</span>
            </div>
          </figure>

          <figure class="training__image">
            <div class="training__image-placeholder">
              <span>Technical Work</span>
            </div>
          </figure>

          <figure class="training__image">
            <div class="training__image-placeholder">
              <span>Chocolate Production</span>
            </div>
          </figure>

          <figure class="training__image">
            <div class="training__image-placeholder">
              <span>Finished Work</span>
            </div>
          </figure>

        </div>

      </div>

    </article>


    <!-- =====================================================
         FELCHLIN
         ===================================================== -->

    <article class="training__institution training__institution--felchlin">

      <!-- Workshop Gallery -->
      <div class="training__gallery-wrap">

        <div class="training__gallery-heading">
          <span>02</span>
          <p>Workshop / Practical</p>
        </div>

        <div class="training__gallery">

          <figure class="training__image">
            <div class="training__image-placeholder">
              <span>Workshop Photo</span>
            </div>
          </figure>

          <figure class="training__image">
            <div class="training__image-placeholder">
              <span>Artisan Work</span>
            </div>
          </figure>

          <figure class="training__image">
            <div class="training__image-placeholder">
              <span>Chocolate Detail</span>
            </div>
          </figure>

          <figure class="training__image">
            <div class="training__image-placeholder">
              <span>Tasting / Study</span>
            </div>
          </figure>

        </div>

      </div>


      <!-- Information -->
      <div class="training__details">

        <header class="training__brand">
          <div class="training__logo training__logo--felchlin">
            <!-- Replace with actual logo -->
            <span>LOGO</span>
          </div>

          <div>
            <p class="training__eyebrow">Chocolate Education</p>

            <h3 class="training__brand-name font-display">
              Felchlin
              <span>Condirama</span>
            </h3>
          </div>
        </header>


        <div class="training__overview">

          <p class="training__label">
            Training Overview
          </p>

          <p class="training__description">
            Advanced exposure to Swiss chocolate craftsmanship, cacao
            appreciation, refined confectionery techniques, and the
            relationship between origin, flavour, texture, and finished
            chocolate work.
          </p>

        </div>


        <div class="training__skills">

          <p class="training__label">
            Core Skills
          </p>

          <ul class="training__skill-list">
            <li>Chocolate craftsmanship</li>
            <li>Cacao &amp; flavour appreciation</li>
            <li>Praline &amp; confectionery work</li>
            <li>Chocolate finishing</li>
            <li>Texture &amp; flavour pairing</li>
            <li>Swiss chocolate techniques</li>
          </ul>

        </div>


        <div class="training__credential">
          <span class="training__credential-mark">✦</span>

          <div>
            <p class="training__label">
              Credential
            </p>

            <p class="training__credential-text">
              Specialist Chocolate Training
            </p>
          </div>
        </div>

      </div>

    </article>

  </div>
</section>


<style>
  /* =========================================================
     SPECIALIZED TRAINING
     ========================================================= */

  .training {
    position: relative;

    padding-block:
      clamp(100px, 13vw, 180px);

    overflow: hidden;
  }


  /* =========================================================
     SECTION INTRO
     ========================================================= */

  .training__intro {
    display: grid;

    grid-template-columns:
      minmax(0, 1.15fr)
      minmax(260px, 0.85fr);

    gap: clamp(48px, 9vw, 140px);

    align-items: end;

    padding-bottom:
      clamp(56px, 7vw, 88px);

    border-bottom: 1px solid var(--color-border);
  }


  .training__section-number {
    display: block;

    margin-bottom: 18px;

    color: var(--color-accent);

    font-size: 10px;

    letter-spacing: 2px;
  }


  .training__intro .eyebrow {
    margin: 0 0 18px;

    color: var(--color-accent);
  }


  .training__title {
    max-width: 8ch;

    font-size:
      clamp(46px, 6vw, 78px);

    line-height: 0.9;

    letter-spacing: -0.025em;
  }


  .training__title em {
    display: block;

    color: var(--color-text-muted);

    font-style: italic;

    font-weight: 300;
  }


  .training__lead {
    max-width: 440px;

    margin: 0;

    color: var(--color-text-muted);

    font-size: var(--text-body-lg);

    line-height: 1.75;
  }


  /* =========================================================
     INSTITUTION
     ========================================================= */

  .training__institution {
    display: grid;

    grid-template-columns:
      minmax(0, 0.85fr)
      minmax(0, 1.15fr);

    gap: clamp(48px, 7vw, 110px);

    padding-block:
      clamp(72px, 9vw, 120px);
  }


  /*
   * Second institution reverses the composition.
   */
  .training__institution--felchlin {
    grid-template-columns:
      minmax(0, 1.15fr)
      minmax(0, 0.85fr);

    border-top: 1px solid var(--color-border);
  }


  .training__institution--felchlin
  .training__details {
    order: 2;
  }


  .training__institution--felchlin
  .training__gallery-wrap {
    order: 1;
  }


  /* =========================================================
     BRAND HEADER
     ========================================================= */

  .training__brand {
    display: flex;

    align-items: center;

    gap: 18px;

    padding-bottom: 32px;

    border-bottom: 1px solid var(--color-border);
  }


  .training__logo {
    width: 58px;
    height: 58px;

    flex-shrink: 0;

    display: flex;

    align-items: center;
    justify-content: center;

    border: 1px solid var(--color-border);

    color: var(--color-text-muted);

    font-size: 8px;

    letter-spacing: 1px;

    /*
     * Deliberately quiet.
     * Real logos can replace this later.
     */
    background: rgba(249, 246, 240, 0.02);
  }


  .training__logo--callebaut {
    border-radius: 50%;
  }


  .training__logo--felchlin {
    border-radius: 2px;
  }


  .training__eyebrow {
    margin: 0 0 5px;

    color: var(--color-accent);

    font-size: 10px;

    text-transform: uppercase;

    letter-spacing: 1.8px;
  }


  .training__brand-name {
    margin: 0;

    color: var(--color-text);

    font-size:
      clamp(28px, 3vw, 42px);

    line-height: 0.95;

    font-weight: 400;
  }


  .training__brand-name span {
    color: var(--color-text-muted);

    font-style: italic;
  }


  /* =========================================================
     TRAINING OVERVIEW
     ========================================================= */

  .training__overview {
    padding-block: 34px;

    border-bottom: 1px solid var(--color-border);
  }


  .training__label {
    margin: 0 0 12px;

    color: var(--color-accent);

    font-size: 10px;
    font-weight: 400;

    text-transform: uppercase;

    letter-spacing: 1.7px;
  }


  .training__description {
    max-width: 48ch;

    margin: 0;

    color: var(--color-text-muted);

    font-size: var(--text-body-lg);

    line-height: 1.75;
  }


  /* =========================================================
     CORE SKILLS
     ========================================================= */

  .training__skills {
    padding-block: 30px;

    border-bottom: 1px solid var(--color-border);
  }


  .training__skill-list {
    display: grid;

    grid-template-columns: repeat(2, minmax(0, 1fr));

    gap: 0;

    margin: 0;

    padding: 0;

    list-style: none;
  }


  .training__skill-list li {
    position: relative;

    padding:
      11px 16px 11px 16px;

    color: var(--color-text);

    font-size: var(--text-body);

    border-bottom: 1px solid rgba(249, 246, 240, 0.06);
  }


  .training__skill-list li::before {
    content: "—";

    position: absolute;

    left: 0;

    color: var(--color-accent);

    opacity: 0.7;
  }


  /* =========================================================
     CREDENTIAL
     ========================================================= */

  .training__credential {
    display: flex;

    align-items: center;

    gap: 14px;

    padding-top: 28px;
  }


  .training__credential-mark {
    display: flex;

    align-items: center;
    justify-content: center;

    width: 26px;
    height: 26px;

    border: 1px solid var(--color-border);

    color: var(--color-accent);

    font-size: 9px;
  }


  .training__credential .training__label {
    margin-bottom: 2px;
  }


  .training__credential-text {
    margin: 0;

    color: var(--color-text);

    font-family: var(--font-display);

    font-size: 18px;
  }


  /* =========================================================
     GALLERY
     ========================================================= */

  .training__gallery-wrap {
    min-width: 0;
  }


  .training__gallery-heading {
    display: flex;

    align-items: center;

    justify-content: space-between;

    margin-bottom: 18px;

    padding-bottom: 12px;

    border-bottom: 1px solid var(--color-border);
  }


  .training__gallery-heading span {
    color: var(--color-accent);

    font-size: 10px;

    letter-spacing: 1.5px;
  }


  .training__gallery-heading p {
    margin: 0;

    color: var(--color-text-muted);

    font-size: 10px;

    text-transform: uppercase;

    letter-spacing: 1.5px;
  }


  /*
   * Desktop 2x2 image arrangement.
   */
  .training__gallery {
    display: grid;

    grid-template-columns: repeat(2, minmax(0, 1fr));

    gap: 8px;
  }


  .training__image {
    position: relative;

    aspect-ratio: 1 / 1;

    margin: 0;

    overflow: hidden;

    background: var(--color-surface);
  }


  .training__image-placeholder {
    width: 100%;
    height: 100%;

    display: flex;

    align-items: center;
    justify-content: center;

    padding: 20px;

    border: 1px solid var(--color-border);

    color: var(--color-text-muted);

    font-size: 10px;

    text-transform: uppercase;

    letter-spacing: 1.5px;

    background:
      linear-gradient(
        135deg,
        rgba(249, 246, 240, 0.025),
        rgba(200, 169, 107, 0.025)
      );

    transition:
      transform 0.6s var(--ease-luxury),
      border-color 0.4s var(--ease-luxury);
  }


  .training__image:hover
  .training__image-placeholder {
    transform: scale(1.025);

    border-color: rgba(200, 169, 107, 0.35);
  }


  /* =========================================================
     TABLET
     ========================================================= */

  @media (max-width: 900px) {

    .training__intro {
      grid-template-columns: 1fr;

      gap: 30px;
    }


    .training__lead {
      max-width: 600px;
    }


    .training__institution,
    .training__institution--felchlin {
      grid-template-columns: 1fr;

      gap: 48px;
    }


    /*
     * Restore natural order on tablet.
     */
    .training__institution--felchlin
    .training__details,
    .training__institution--felchlin
    .training__gallery-wrap {
      order: initial;
    }
  }


  /* =========================================================
     MOBILE
     ========================================================= */

  @media (max-width: 640px) {

    .training {
      padding-block: 80px;
    }


    .training__intro {
      gap: 24px;

      padding-bottom: 46px;
    }


    .training__title {
      font-size:
        clamp(44px, 13vw, 60px);
    }


    .training__lead {
      font-size: 15px;

      line-height: 1.7;
    }


    /*
     * Each institution becomes its own clearly
     * separated editorial block.
     */
    .training__institution,
    .training__institution--felchlin {
      display: flex;

      flex-direction: column;

      gap: 38px;

      padding-block: 56px;
    }


    .training__institution--felchaut {
      border-top: 1px solid var(--color-border);
    }


    .training__institution--felchlin {
      padding-top: 64px;
    }


    /*
     * Required mobile order:
     *
     * Header
     * Overview
     * Skills
     * Photos
     */

    .training__details {
      display: contents;
    }


    .training__brand {
      order: 1;

      padding-bottom: 24px;
    }


    .training__overview {
      order: 2;

      padding-block: 0 28px;
    }


    .training__skills {
      order: 3;

      padding-block: 0 28px;
    }


    .training__credential {
      order: 4;

      padding-top: 0;
    }


    .training__gallery-wrap {
      order: 5;

      width: calc(100vw - 40px);
    }


    /*
     * Horizontal touch gallery.
     *
     * The next image intentionally peeks into view.
     */
    .training__gallery {
      display: flex;

      gap: 10px;

      overflow-x: auto;

      overscroll-behavior-x: contain;

      scroll-snap-type: x mandatory;

      scrollbar-width: none;

      padding-right: 24px;

      -webkit-overflow-scrolling: touch;
    }


    .training__gallery::-webkit-scrollbar {
      display: none;
    }


    .training__image {
      flex: 0 0 78%;

      aspect-ratio: 4 / 5;

      scroll-snap-align: start;
    }


    .training__image-placeholder {
      font-size: 9px;
    }


    .training__skill-list {
      grid-template-columns: 1fr;
    }


    .training__skill-list li {
      padding-block: 10px;
    }


    .training__brand-name {
      font-size: 30px;
    }


    .training__logo {
      width: 50px;
      height: 50px;
    }
  }


  /* =========================================================
     SMALL PHONES
     ========================================================= */

  @media (max-width: 400px) {

    .training {
      padding-block: 68px;
    }


    .training__institution,
    .training__institution--felchlin {
      gap: 32px;

      padding-block: 48px;
    }


    .training__institution--felchlin {
      padding-top: 56px;
    }


    .training__brand {
      gap: 13px;
    }


    .training__logo {
      width: 46px;
      height: 46px;
    }


    .training__brand-name {
      font-size: 27px;
    }


    .training__gallery-wrap {
      width: calc(100vw - 32px);
    }


    .training__image {
      flex-basis: 82%;
    }
  }


  /* =========================================================
     REDUCED MOTION
     ========================================================= */

  @media (prefers-reduced-motion: reduce) {

    .training__image-placeholder,
    .training__institution,
    .training__gallery {
      scroll-behavior: auto;
      transition: none;
    }
  }
</style>