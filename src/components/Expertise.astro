<section class="expertise" id="expertise">
  <div class="container">

    <div class="expertise__header">
      <div class="expertise__heading">
        <span class="expertise__index">02</span>

        <p class="eyebrow">Areas of Expertise</p>

        <h2 class="font-display">
          Craft shaped by experience,
          <em>refined through precision.</em>
        </h2>
      </div>

      <p class="expertise__intro">
        A broad pastry and bakery practice developed across luxury hotel
        kitchens, combining classical foundations with contemporary
        presentation, production discipline, and team leadership.
      </p>
    </div>


    <div class="expertise__list">

      <article class="expertise__item">
        <span class="expertise__number">01</span>

        <div class="expertise__content">
          <h3>Plated Desserts</h3>

          <p>
            Contemporary plated desserts balancing texture, flavour,
            temperature, composition, and refined presentation.
          </p>
        </div>

        <span class="expertise__mark" aria-hidden="true">✦</span>
      </article>


      <article class="expertise__item">
        <span class="expertise__number">02</span>

        <div class="expertise__content">
          <h3>Cake &amp; Entremet</h3>

          <p>
            Layered cakes and entremets combining precise construction,
            balanced flavour profiles, and elegant finishing.
          </p>
        </div>

        <span class="expertise__mark" aria-hidden="true">✦</span>
      </article>


      <article class="expertise__item">
        <span class="expertise__number">03</span>

        <div class="expertise__content">
          <h3>Chocolate &amp; Confectionery</h3>

          <p>
            Chocolate bonbons, tempered chocolate work, decorative
            elements, and large-scale chocolate showpieces.
          </p>
        </div>

        <span class="expertise__mark" aria-hidden="true">✦</span>
      </article>


      <article class="expertise__item">
        <span class="expertise__number">04</span>

        <div class="expertise__content">
          <h3>Viennoiserie &amp; Bakery</h3>

          <p>
            Artisan breads, laminated doughs, croissants, pain au chocolat,
            and consistent bakery production.
          </p>
        </div>

        <span class="expertise__mark" aria-hidden="true">✦</span>
      </article>


      <article class="expertise__item">
        <span class="expertise__number">05</span>

        <div class="expertise__content">
          <h3>Menu Development</h3>

          <p>
            Developing seasonal pastry selections with attention to
            production feasibility, presentation, and guest experience.
          </p>
        </div>

        <span class="expertise__mark" aria-hidden="true">✦</span>
      </article>


      <article class="expertise__item">
        <span class="expertise__number">06</span>

        <div class="expertise__content">
          <h3>Operations &amp; Leadership</h3>

          <p>
            Team supervision, production planning, quality control,
            hygiene standards, inventory discipline, and service readiness.
          </p>
        </div>

        <span class="expertise__mark" aria-hidden="true">✦</span>
      </article>

    </div>

  </div>
</section>


<style>
  /* =====================================================
     EXPERTISE
     ===================================================== */

  .expertise {
    position: relative;

    padding-block:
      clamp(90px, 1vw, 160px);

    overflow: hidden;
  }


  /* =====================================================
     HEADER
     ===================================================== */

  .expertise__header {
    display: grid;

    grid-template-columns:
      minmax(0, 1.25fr)
      minmax(260px, 0.75fr);

    gap: clamp(48px, 8vw, 120px);

    align-items: end;

    padding-bottom: clamp(48px, 7vw, 80px);

    border-bottom: 1px solid var(--color-border);
  }


  .expertise__heading {
    position: relative;
  }


  .expertise__index {
    display: block;

    margin-bottom: 18px;

    color: var(--color-accent);

    font-family: var(--font-body);

    font-size: 10px;
    font-weight: 400;

    letter-spacing: 2px;
  }


  .expertise__heading .eyebrow {
    margin: 0 0 18px;

    color: var(--color-accent);
  }


  .expertise__heading h2 {
    max-width: 700px;

    font-size:
      clamp(34px, 4vw, 58px);

    line-height: 1.02;

    letter-spacing: -0.02em;
  }


  .expertise__heading h2 em {
    color: var(--color-text-muted);

    font-style: italic;
    font-weight: 300;
  }


  .expertise__intro {
    max-width: 410px;

    margin: 0;

    color: var(--color-text-muted);

    font-size: var(--text-body-lg);

    line-height: 1.75;
  }


  /* =====================================================
     EXPERTISE LIST
     ===================================================== */

  .expertise__list {
    display: grid;

    grid-template-columns: repeat(2, minmax(0, 1fr));
  }


  /* =====================================================
     EXPERTISE ITEM
     ===================================================== */

  .expertise__item {
    position: relative;

    display: grid;

    grid-template-columns: 42px minmax(0, 1fr) 24px;

    column-gap: 20px;

    min-width: 0;

    padding:
      clamp(28px, 4vw, 44px)
      clamp(8px, 2vw, 24px);

    border-bottom: 1px solid var(--color-border);

    transition:
      background 0.4s var(--ease-luxury),
      padding-left 0.4s var(--ease-luxury);
  }


  /*
   * Vertical separator between columns.
   */
  .expertise__item:nth-child(odd) {
    border-right: 1px solid var(--color-border);
  }


  /*
   * Subtle editorial hover.
   */
  .expertise__item:hover {
    background: rgba(200, 169, 107, 0.025);
border-left: 4px solid var(--color-accent);
    padding-left:
      clamp(14px, 2.5vw, 30px);
  }


  /* =====================================================
     NUMBER
     ===================================================== */

  .expertise__number {
    padding-top: 4px;

    color: var(--color-accent);

    font-size: 10px;

    letter-spacing: 1.5px;
  }


  /* =====================================================
     CONTENT
     ===================================================== */

  .expertise__content {
    min-width: 0;
  }


  .expertise__content h3 {
    margin: 0;

    color: var(--color-text);

    font-family: var(--font-display);

    font-size:
      clamp(21px, 1.8vw, 28px);

    font-weight: 400;

    line-height: 1.15;

    transition:
      color 0.3s var(--ease-luxury);
  }


  .expertise__item:hover .expertise__content h3 {
    color: var(--color-accent-hover);
  }


  .expertise__content p {
    max-width: 48ch;

    margin:
      12px 0 0;

    color: var(--color-text-muted);

    font-size: var(--text-body);

    line-height: 1.7;
  }


  /* =====================================================
     EDITORIAL MARK
     ===================================================== */

  .expertise__mark {
    align-self: start;

    padding-top: 3px;

    color: var(--color-accent);

    font-size: 20px;

    opacity: 0.45;

    transition:
      opacity 0.35s var(--ease-luxury),
      transform 0.35s var(--ease-luxury);
  }


  .expertise__item:hover .expertise__mark {
    opacity: 1;

    transform: rotate(45deg);
  }


  /* =====================================================
     TABLET
     ===================================================== */

  @media (max-width: 900px) {

    .expertise__header {
      grid-template-columns: 1fr;

      gap: 28px;

      align-items: start;
    }


    .expertise__intro {
      max-width: 600px;
    }


    .expertise__item {
      grid-template-columns:
        34px minmax(0, 1fr) 18px;

      column-gap: 14px;
    }
  }


  /* =====================================================
     MOBILE
     ===================================================== */

  @media (max-width: 640px) {

    .expertise {
      padding-block: 40px;
    }


    .expertise__header {
      padding-bottom: 42px;
    }


    .expertise__heading h2 {
      max-width: 13ch;

      font-size:
        clamp(34px, 10vw, 46px);
    }


    .expertise__intro {
      max-width: 48ch;

      font-size: 15px;

      line-height: 1.7;
    }


    /*
     * One-column editorial list.
     */
    .expertise__list {
      grid-template-columns: 1fr;
    }


    /*
     * Remove desktop column divider.
     */
    .expertise__item:nth-child(odd) {
      border-right: 0;
    }


    .expertise__item {
      grid-template-columns:
        32px minmax(0, 1fr) 16px;

      gap: 14px;

      padding:
        28px 0;
    }


    .expertise__item:hover {
      padding-left: 6px;
    }


    .expertise__content h3 {
      font-size: 23px;
    }


    .expertise__content p {
      margin-top: 9px;

      line-height: 1.65;
    }
  }


  /* =====================================================
     SMALL PHONES
     ===================================================== */

  @media (max-width: 400px) {

    .expertise {
      padding-block: 20px;
    }


    .expertise__heading h2 {
      font-size: 34px;
    }
.expertise__mark {
  font-size: 14px;
}

    .expertise__item {
      grid-template-columns:
        28px minmax(0, 1fr) 12px;

      column-gap: 10px;
    }


    .expertise__content h3 {
      font-size: 21px;
    }


    .expertise__content p {
      font-size: 13px;
    }
  }


  /* =====================================================
     REDUCED MOTION
     ===================================================== */

  @media (prefers-reduced-motion: reduce) {

    .expertise__item,
    .expertise__content h3,
    .expertise__mark {
      transition: none;
    }
  }
</style>